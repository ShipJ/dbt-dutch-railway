[project]
name = "dbt-dutch-railway"
version = "0.1.0"
description = "Dutch railway analysis with dbt + DuckDB + postgres + lightdash"
readme = "README.md"
requires-python = ">=3.10,<3.13"
dependencies = [
    "dbt-core>=1.10,<1.11",
    "dbt-duckdb==1.9.6",
    "duckdb>=1.0.0",
]

